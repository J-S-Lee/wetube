extends layouts/main

block content
    .form-container
        form(action=routes.login, method="post")
            input(type="email", name="email", paceholder="Email", required=true)
            input(type="password", name="password", paceholder="Password", required=true)
            input(type="submit", value="Log In")
        include partials/socialLogin